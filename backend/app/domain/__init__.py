"""
YUGI-AI — Domain Layer
========================

Pure business rules with zero external dependencies.
This package must NEVER import from infrastructure, api, or middleware.

Contains:
    - Base entity and domain event classes
    - Repository and service interfaces (ports)
    - Future: Feature-specific domain entities and value objects
"""
